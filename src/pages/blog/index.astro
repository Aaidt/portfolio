---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';
import Header from '../../components/Header.astro';
import { SITE_TITLE } from '../../consts';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`Blog | ${SITE_TITLE}`} description="Technical notes and experiments from Aadit Suryawanshi." />
	</head>
	<body>
		<main>
			<section class="blog-hero">
				<div class="container">
					<p class="kicker">Blog</p>
					<h1>Notes from the lab.</h1>
					<p>Long-form writeups on language models, training systems, architecture choices, and what broke along the way.</p>
				</div>
			</section>
			<section class="posts-section">
				<div class="container posts-grid">
					{posts.map((post) => (
						<a class="post-card" href={`${import.meta.env.BASE_URL}blog/${post.id}/`}>
							{post.data.heroImage && <Image width={900} height={480} src={post.data.heroImage} alt="" />}
							<div>
								<p class="date"><FormattedDate date={post.data.pubDate} /></p>
								<h2>{post.data.title}</h2>
								<p>{post.data.description}</p>
							</div>
						</a>
					))}
				</div>
			</section>
		</main>
		<!-- <Footer /> -->
		<Header />
	</body>
</html>

<style>
	.blog-hero {
		background: var(--paper-deep);
	}
	.blog-hero p:not(.kicker) {
		max-width: 680px;
		font-size: 1.2rem;
	}
	.posts-grid {
		display: grid;
		grid-template-columns: 1fr;
		gap: 1rem;
		max-width: 620px;
		margin: 0 auto;
	}
	.post-card {
		display: grid;
		gap: 0.9rem;
		padding: 0 0 1rem;
		border: none;
		border-bottom: 1px solid var(--line);
		border-radius: 0;
		background: transparent;
		color: rgb(var(--gray-dark));
		text-decoration: none;
	}
	.post-card:hover {
		border-color: var(--line);
		box-shadow: none;
		transform: none;
	}
	.post-card img {
		width: 100%;
		aspect-ratio: 16 / 8;
		object-fit: cover;
	}
	.post-card h2 {
		font-size: clamp(1.3rem, 3vw, 2rem);
	}
	.date {
		margin-bottom: 0.4rem;
		color: var(--accent);
		font-size: 0.9rem;
	}
</style>
